<p id="notice"><%= notice %></p>

<h1>Listing Books</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Published date</th>
      <th>Author</th>
      <th>Category</th>
    </tr>
  </thead>

  <tbody>
    <% @books.each do |book| %>
      <tr>
        <td><%= link_to book.title, book %></td>
        <td><%= book.published_date %></td>
        <td><%= book.author %></td>
        <td><%= book.category %></td>
      </tr>
    <% end %>
    <%= paginate @books %>
  </tbody>
</table>
